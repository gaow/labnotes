gw_log
==========================
Gao Wang (*) 2012, GNU GPL

Dependencies
------------
sudo apt-get install texlive texlive-xetex texlive-latex-extra python-pygments latexmk

Installation
------------
cd path/to/some/folder
svn co svn+ssh://usrname@csg.bcmd.bcm.edu/nfs/usr/local/svn/gw_log ./
svn up
sudo python3 setup.py install

Help
----
gw_log -h

DOC Example
-----------
gw_log doc -h
gw_log doc demo.notes --author "John Doe" --title "Example log documentation" -f --lite --output d1.pdf
gw_log doc demo.notes --author "John Doe" --title "Example log documentation" --toc --output d2.pdf

SLIDES Example
--------------
gw_log slides -h
gw_log slides demo.snotes -o d3
gw_log slides demo.snotes --theme plain -o d4
gw_log slides demo.snotes -a "John Doe" -t "Example log slides" -i "McDonald's" --toc -o d5
gw_log slides demo.snotes -a "John Doe" -t "Example log slides" -i "McDonald's" --toc --theme heavy -o d6
gw_log slides demo.snotes -a "John Doe" -t "Example log slides" --toc --mode handout -o d7
gw_log slides demo.snotes -a "John Doe" -t "Example log slides" --toc --mode notes -o d8

Syntax
------
###  ###				#{ ----------	#{ out	#{ xxx (xxx = warning, important, tip, note)	#{ list		#{ xxx (xxx = latex, bash, r, cpp, python, sql)
#!   #								#						#(or ##)
###, ###, #*, #!, #!!, #!!!, #, 	#} ----------,  #}	#}     						#} 		#}

Emphasis: ``double quote'', `single quote',  "italics", ""bold"", """bold italics""", @@monospaced@@, @url@, [keyword|footnote]
